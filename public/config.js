// Configuración de ejecución del panel.
//
// Este archivo es un marcador de posición: en desarrollo queda vacío y la
// aplicación cae a los valores de `.env.development`. En los contenedores,
// `docker/30-config.sh` lo reescribe al arrancar con las variables reales del
// ambiente, para que una misma imagen sirva en staging y en producción.
window.__ENV__ = {};
